# Purple Protocol Core Library

This repository contains the protocol building blocks.
It is intentionally structured as a reusable foundation
for future applications such as a CLI, GUI clients,
embedded SDKs, or dedicated server runtimes.

